<h1>投稿編集フォーム</h1>

<%= form_with model: @post do |f| %>
    <h4>画像</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>タイトル</h4>
      <%= f.text_field :title %>
    <h4>説明文</h4>
      <%= f.text_area :introduction %>
    <%= f.submit '更新' %>
<% end %>